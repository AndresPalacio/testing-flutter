import 'package:flutter_test/flutter_test.dart';
import 'package:project_test_unit/bloc/bottom_nav_bloc.dart';

void main(List<String> args) {
  
  test("picklItem debe devolver el nombre de la appbar de la aplicacion", (){
      int numberElement = 0;
  //    String response = BottomNavBarBloc.pickItem(numberElement);
    //  expect(NavBarItem.HOME, response);
  });
}